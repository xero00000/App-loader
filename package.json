{
  "name": "netlify-app-gallery",
  "version": "1.0.0",
  "description": "A gallery to host and display HTML apps on Netlify.",
  "main": "build.js",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  }
}
